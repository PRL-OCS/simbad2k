version: '3.7'

services:
  simbad2k:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: simbad2k
    networks:
      - ocsnet
    ports:
      - "5000:5000"
    env_file:
      - .env
    mem_limit: "512m"
    restart: always

networks:
  ocsnet:
    external: true
